import utils from './utils';

let $touch;
let $w = window;
let _config = {};

/**
 * 主方法
 * @param _e    受控节点
 * @param _opt  配置参数
 */
$touch = function (_e, _opt) {

};

/**
 * 配置
 * @returns {{}}
 */
$touch.prototype.config = function () {
    return _config;
};

